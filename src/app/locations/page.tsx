import React from "react"; import { Metadata } from "next"; import Locations from "@/components/Locations"; export const metadata: Metadata = { title: "Our Locations", description: "Find Apexy Packers and Movers branches across India. Contact our nearest branch for professional packing, moving, and relocation services." }; export default function LocationsPage() { return ( <main><Locations /></main> ); }
